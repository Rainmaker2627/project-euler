This is quite easily solved by the Sieve of Eratosthenes. To find the $N$th prime, we simply have to sieve the numbers up to
some upper bound. Luckily, by the prime number theorem, if $p_n$ is the $n$th prime, then
$$n\left(\log n+\log\log n-1\right)< p_n < n(\log n+\log\log n)$$
for all $n\ge 6$.